<%- model_class = FantasyPoint -%>
<div class="page-header">
  <h1>Fantasy Baseball Point Assignments</h1>
  <p>
    The system currently has the fantasy baseball point assignments shown
    below. If you would like to change them you may use the <strong>
    Edit</strong> button. If you would like to delete them, use the 
    <strong>Delete</strong> button.
  </p>
</div>

<dl class="dl-horizontal">
  <dt><strong>Home Runs:</strong></dt>
  <dd><%= @fantasy_point.home_run %></dd>
  <dt><strong>RBI's:</strong></dt>
  <dd><%= @fantasy_point.rbi %></dd>
  <dt><strong>Stolen Bases:</strong></dt>
  <dd><%= @fantasy_point.stolen_base %></dd>
  <dt><strong>Caught Stealing:</strong></dt>
  <dd><%= @fantasy_point.caught_stealing %></dd>
</dl>

<div class="form-actions">
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_fantasy_point_path(@fantasy_point), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              fantasy_point_path(@fantasy_point),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
